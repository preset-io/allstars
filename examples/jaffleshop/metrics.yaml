metrics:
- key: order_distinct_count
  label: Some metric someone defined
  expression: COUNT(DISTINCT jaffleshop.orders.order_id)
  relation_keys:
  - jaffleshop.orders
- key: jaffleshop.customers.count
  label: customers count
  description: the number of {r.reference}
  expression: COUNT(*)
  relation_keys:
  - jaffleshop.customers
- key: jaffleshop.orders.count
  label: orders count
  description: the number of {r.reference}
  expression: COUNT(*)
  relation_keys:
  - jaffleshop.orders
- key: jaffleshop.raw_customers.count
  label: raw_customers count
  description: the number of {r.reference}
  expression: COUNT(*)
  relation_keys:
  - jaffleshop.raw_customers
- key: jaffleshop.raw_orders.count
  label: raw_orders count
  description: the number of {r.reference}
  expression: COUNT(*)
  relation_keys:
  - jaffleshop.raw_orders
- key: jaffleshop.raw_payments.count
  label: raw_payments count
  description: the number of {r.reference}
  expression: COUNT(*)
  relation_keys:
  - jaffleshop.raw_payments
- key: jaffleshop.stg_customers.count
  label: stg_customers count
  description: the number of {r.reference}
  expression: COUNT(*)
  relation_keys:
  - jaffleshop.stg_customers
- key: jaffleshop.stg_orders.count
  label: stg_orders count
  description: the number of {r.reference}
  expression: COUNT(*)
  relation_keys:
  - jaffleshop.stg_orders
- key: jaffleshop.stg_payments.count
  label: stg_payments count
  description: the number of {r.reference}
  expression: COUNT(*)
  relation_keys:
  - jaffleshop.stg_payments
